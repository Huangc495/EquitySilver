# Pinned to Databricks serverless environment 4 (Python 3.12.3), as reported
# by notebooks/00a_cluster_preflight on the workspace on 2026-09-22 (D-01).
# torch, mlflow and openpyxl are not part of that environment; the cluster
# gets them from requirements-databricks.txt at the same versions (D-33).
numpy==2.1.3
pandas==2.2.3
pyarrow==19.0.1
torch==2.6.0
matplotlib==3.10.0
openpyxl==3.1.2
PyYAML==6.0.2
mlflow==2.21.3
scipy==1.15.1
pytest==8.3.5
